# Credentials — never commit these
apikey
anthropic_key
*_key
creds.json
.env
.env.*
*.pem


# This project has no build step and no dependencies — kept as a guard so a
# stray `npm install` cannot get committed.
node_modules/

# Regenerated by `node --experimental-websocket tests/click_through.js`
tests/screenshots/
